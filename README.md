# Lucid

Escrow and transaction management app built with React Native (Expo SDK 55). Handles transaction creation, counterparty management, settlements, and document uploads.

To get started, clone the repo, run `npm install`, copy `.env.example` to `.env` and fill in your API credentials, then `npx expo start`.
